#include <stdio.h>
#include <string.h>
int main()
{
	char a[100000];
	char daan[100000];
	scanf("%s",&a);
	int i,n,m;
	n=strlen(a);
	int zhong;
	int b=(int)a[0];
	for (int i=1;i<n;i++)
		{
			m=0+(int)a[i];
			daan[i-1]=a[i];
			
				if(m==b and )
					{
						printf("%d",i);
					}
		}
 } 
